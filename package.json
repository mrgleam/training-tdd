{
  "scripts": {
    "start": "fusuma start",
    "build": "fusuma build",
    "pdf": "fusuma pdf",
    "live": "fusuma live -w '@nodejs'",
    "deploy": "fusuma deploy"
  },
  "devDependencies": {
    "fusuma": "^1.15.0"
  }
}
